# 21day
我的app设计尝试-个人使用

This is a code bundle for Word and Sentence Memory Card. The original project is available at https://www.figma.com/design/vzvnv63scp4c9cyvJeQ4om/Word-and-Sentence-Memory-Card.

## Running the code

Run `npm i` to install the dependencies.
React and React DOM are included in dependencies.

Run `npm run dev` to start the development server.
